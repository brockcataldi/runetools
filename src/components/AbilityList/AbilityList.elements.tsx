import { styled } from 'styled-components'

const Wrapper = styled.div`
    display: grid;
    grid-template-columns: repeat(6, 32px);
    gap: 1rem;
    width: 274px;
    margin: 0 auto;
`

export { Wrapper }